import 'package:flutter/material.dart';

class Sell extends StatelessWidget {
  Sell({Key? key}) : super(key: key); // Initializes key for subclasses.

  @override
  Widget build(BuildContext context) {
    return Text('data');
  }
}
